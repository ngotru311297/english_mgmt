# Workspace Copilot Instructions

- Create a React project named `class_mgmt` using Vite and TypeScript.
- Add starter source files for `App`, `main`, and basic styling.
- Keep the project minimal and ready to run with `npm install`.
